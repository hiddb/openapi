description: successful operation
content:
  application/json:
    schema:
      type: object
      additionalProperties: false
      required:
        - access_token
        - type
      properties:
        access_token:
          type: string
        type:
          type: string
          enum: ["Bearer"]