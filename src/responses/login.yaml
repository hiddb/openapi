description: successful operation
content:
  application/json:
    schema:
      type: object
      additionalProperties: false
      properties:
        access_token:
          type: string
        type:
          type: string
          enum: ["Bearer"]