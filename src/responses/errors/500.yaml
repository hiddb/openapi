description: internal server error
content:
  application/json:
    schema:
      type: object 
      nullable: true
      additionalProperties: true
      required:
        - error
      properties:
        error:
          type: string